/* import { defineConfig } from 'vite'

export default defineConfig({
  base: './',
  build: {
    outDir: 'dist',
    rollupOptions: {
      input: {
        main: './index.html',
       
        homepage: './src/homepage/homepage.html',
        register: './src/registerform/registerform.html',
        login: './src/login/login.html',
        userpage: './src/userpage/userpage.html',
      },
    },
  },
  publicDir: 'module-L2',
})
 */

import { defineConfig } from 'vite'

export default defineConfig({
  
  base: './',

  
  build: {
    outDir: 'dist', 
  },

})
